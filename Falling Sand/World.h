#pragma once
// Manages the simulation grid state and the core particle update logic.
// Particle type logic will be in here for easy access to both grids and all cells.
// Split each particle update logic into own func then call all funcs in one overall func
class World
{
};

